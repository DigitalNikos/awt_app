import 'package:flutter/material.dart';

class AppColors {
  // Primary color - a rich blue similar to Vienna Airport's brand
  static const Color primary = Color(0xFF003B70);

  // Secondary color - accent color for buttons, etc.
  static const Color secondary = Color(0xFFE4032E);

  // Background colors
  static const Color background = Colors.white;
  static const Color lightBackground = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF202020);
  static const Color textSecondary = Color(0xFF606060);
  static const Color textLight = Color(0xFF909090);

  // Utility colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFAB00);
  static const Color info = Color(0xFF2196F3);
}
